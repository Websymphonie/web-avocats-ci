{% extends 'layouts/base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <main class="flex-1 bg-muted/30 px-4 py-6 sm:px-6 lg:px-8 lg:py-8">
        <div class="mx-auto max-w-3xl space-y-6">
            {{ component('Breadcrumbs', {'breadcrumbs': breadcrumbs}) }}
            <section class="rounded-2xl border border-border bg-card p-6 shadow-sm sm:p-8">
                <div class="border-b border-border pb-6">
                    <p class="text-sm font-medium text-muted-foreground">Nouveau compte</p>
                    <h1 class="mt-1 text-2xl font-semibold tracking-[-0.03em]">Ajouter un utilisateur</h1>
                    <p class="mt-2 text-sm text-muted-foreground">Créez les accès d’un collaborateur et définissez son niveau d’autorisation.</p>
                </div>

                {{ form_start(form, {'attr': {'class': 'mt-7 space-y-6', 'data-loading-label': 'Création…'}}) }}
                {{ form_errors(form) }}
                <div class="space-y-2">
                    {{ form_label(form.name, null, {'label_attr': {'class': 'text-sm font-medium'}}) }}
                    {{ form_widget(form.name, {'attr': {'class': 'flex h-11 w-full rounded-lg border border-input bg-background px-3 text-sm shadow-xs outline-none focus-visible:ring-2 focus-visible:ring-ring/30', 'autocomplete': 'name'}}) }}
                    <div class="text-sm text-destructive">{{ form_errors(form.name) }}</div>
                </div>
                <div class="space-y-2">
                    {{ form_label(form.email, null, {'label_attr': {'class': 'text-sm font-medium'}}) }}
                    {{ form_widget(form.email, {'attr': {'class': 'flex h-11 w-full rounded-lg border border-input bg-background px-3 text-sm shadow-xs outline-none focus-visible:ring-2 focus-visible:ring-ring/30'}}) }}
                    <div class="text-sm text-destructive">{{ form_errors(form.email) }}</div>
                </div>
                <p class="rounded-xl border border-border bg-muted/40 p-4 text-sm text-muted-foreground">Le compte sera activé par un lien à usage unique envoyé par e-mail. L’utilisateur choisira lui-même son mot de passe.</p>
                <fieldset class="space-y-3">
                    <legend class="text-sm font-medium">{{ form.roles.vars.label }}</legend>
                    <div class="grid gap-3 sm:grid-cols-2">
                        {% for role in form.roles %}
                            <label class="flex cursor-pointer items-center gap-3 rounded-xl border border-border p-4 transition-colors hover:bg-muted/50" for="{{ role.vars.id }}">
                                {{ form_widget(role, {'attr': {'class': 'size-4 rounded border-input text-primary'}}) }}
                                <span class="text-sm font-medium">{{ role.vars.label }}</span>
                            </label>
                        {% endfor %}
                    </div>
                    <div class="text-sm text-destructive">{{ form_errors(form.roles) }}</div>
                </fieldset>
                <div>
                    <label class="flex items-start gap-3 rounded-xl border border-border p-4" for="{{ form.sendMail.vars.id }}">
                        {{ form_widget(form.sendMail, {'attr': {'class': 'mt-0.5 size-4 rounded border-input text-primary'}}) }}
                        <span><span class="block text-sm font-medium">Envoyer le lien d’activation</span><span class="mt-1 block text-xs text-muted-foreground">Le lien expire après 24 heures.</span></span>
                    </label>
                </div>
                <div class="flex flex-col-reverse gap-3 border-t border-border pt-6 sm:flex-row sm:justify-end">
                    <a class="inline-flex h-10 items-center justify-center rounded-lg border border-border px-4 text-sm font-medium hover:bg-muted" href="{{ path('app_user_index') }}">Annuler</a>
                    <button class="inline-flex h-10 items-center justify-center rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground hover:bg-primary/90" type="submit">Créer le compte</button>
                </div>
                {{ form_end(form) }}
            </section>
        </div>
    </main>
{% endblock %}
